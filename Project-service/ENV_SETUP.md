# 🔧 การตั้งค่า Environment Variables

## ⚠️ สำคัญ: การตั้งค่า NEXT_PUBLIC_API_URL

### ✅ แนะนำ: ใช้ Next.js Proxy (แนะนำ)

ในไฟล์ `.env.local` ควรตั้งค่าเป็น:

```env
# ใช้ Next.js proxy (แนะนำ)
NEXT_PUBLIC_API_URL=/api
```

หรือ **ไม่ต้องตั้งเลย** (จะใช้ default `/api`)

### ❌ ไม่แนะนำ: เชื่อมต่อตรง

ถ้าต้องการเชื่อมต่อตรงไปที่ Backend (ไม่ผ่าน proxy):

```env
# เชื่อมต่อตรงไปที่ Backend (ต้องแก้ port ให้ตรงกับ Backend)
NEXT_PUBLIC_API_URL=http://localhost:3001
```

⚠️ **หมายเหตุ**: 
- ถ้า Backend ทำงานที่ port 3001 ต้องตั้งเป็น `http://localhost:3001`
- ถ้า Backend ทำงานที่ port 3002 ต้องตั้งเป็น `http://localhost:3002`
- แต่แนะนำให้ใช้ `/api` เพื่อให้ Next.js proxy ทำงาน (แก้ไข port ที่เดียวใน `next.config.mjs`)

---

## 📝 วิธีแก้ไข

1. เปิดไฟล์ `.env.local` ในโฟลเดอร์ `Project-service`
2. เปลี่ยนจาก:
   ```env
   NEXT_PUBLIC_API_URL=http://localhost:3002
   ```
   
   เป็น:
   ```env
   NEXT_PUBLIC_API_URL=/api
   ```
   
   หรือลบบรรทัดนี้ออกเลย (จะใช้ default)

3. **Restart Next.js dev server** เพื่อให้การเปลี่ยนแปลงมีผล

---

## 🔍 ตรวจสอบการตั้งค่า

หลังจาก restart แล้ว เปิด Browser Console (F12) และดู:
- ✅ `[ApiClient] Initialized with baseURL: /api` = ใช้ proxy ถูกต้อง
- ✅ `[ApiClient] Using Next.js proxy - requests will be forwarded to http://localhost:3001`

---

## 📋 สรุป

| การตั้งค่า | ผลลัพธ์ |
|-----------|---------|
| `NEXT_PUBLIC_API_URL=/api` | ✅ ใช้ Next.js proxy (แนะนำ) |
| `NEXT_PUBLIC_API_URL=http://localhost:3001` | ⚠️ เชื่อมต่อตรง (ต้องแก้ port เอง) |
| ไม่ตั้งค่า | ✅ ใช้ default `/api` (แนะนำ) |
