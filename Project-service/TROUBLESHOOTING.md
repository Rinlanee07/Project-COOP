# 🔧 แก้ไขปัญหา: ไม่สามารถเชื่อมต่อกับเซิร์ฟเวอร์

## ⚠️ ปัญหา
เห็นข้อความ: **"ไม่สามารถเชื่อมต่อกับเซิร์ฟเวอร์ได้ กรุณาตรวจสอบว่าเซิร์ฟเวอร์ทำงานอยู่หรือไม่"**

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: ตรวจสอบว่า Backend ทำงานอยู่หรือไม่

เปิด Terminal/PowerShell ใหม่และรัน:

```bash
# ตรวจสอบ port 3001
netstat -ano | findstr :3001
```

**ถ้าไม่มีผลลัพธ์** = Backend ยังไม่ทำงาน → ไปขั้นตอนที่ 2

**ถ้ามีผลลัพธ์** = Backend ทำงานอยู่ → ไปขั้นตอนที่ 3

---

### ขั้นตอนที่ 2: เริ่ม Backend Server

1. เปิด Terminal/PowerShell ใหม่
2. ไปที่โฟลเดอร์ backend:
   ```bash
   cd backend
   ```
3. เริ่ม Backend:
   ```bash
   npm run start:dev
   ```
4. รอจนเห็นข้อความ: **"Backend running on port 3001"** (หรือ port อื่นที่ตั้งค่าไว้)

⚠️ **สำคัญ**: ปล่อย Terminal นี้เปิดไว้ อย่าปิด!

---

### ขั้นตอนที่ 3: ตรวจสอบ Next.js Dev Server

1. เปิด Terminal/PowerShell อีกตัวหนึ่ง
2. ไปที่โฟลเดอร์ Project-service:
   ```bash
   cd Project-service
   ```
3. เริ่ม Next.js (ถ้ายังไม่ทำงาน):
   ```bash
   npm run dev
   ```
4. **ถ้า Next.js ทำงานอยู่แล้ว** ให้หยุด (Ctrl+C) และเริ่มใหม่เพื่อให้ `next.config.mjs` มีผล

---

### ขั้นตอนที่ 4: ตรวจสอบ Browser Console

1. เปิด Browser (Chrome/Edge)
2. กด F12 เพื่อเปิด Developer Tools
3. ไปที่แท็บ **Console**
4. ดู logs:
   - ✅ `[ApiClient] Initialized with baseURL: /api` = ใช้ proxy ถูกต้อง
   - ✅ `[ApiClient] Making request:` = กำลังเรียก API
   - ❌ `[ApiClient] Request exception:` = มีปัญหา

---

## 📋 Checklist

- [ ] Backend ทำงานอยู่ (เห็น "Backend running on port 3001" หรือ port ที่ตั้งค่าไว้)
- [ ] Next.js ทำงานอยู่ (เห็น "Ready" และ port 3000)
- [ ] Browser Console แสดง `[ApiClient] Initialized with baseURL: /api`
- [ ] ไม่มี error ใน Browser Console

---

## 🐛 ปัญหาที่พบบ่อย

### 1. Port 3001 ถูกใช้งานแล้ว
```bash
# หา process ที่ใช้ port 3001
netstat -ano | findstr :3001
# ดู PID แล้ว kill process (แทนที่ <PID> ด้วยตัวเลขที่เจอ)
taskkill /PID <PID> /F
```

### 2. Database ไม่ได้เชื่อมต่อ
- ตรวจสอบว่า PostgreSQL ทำงานอยู่
- ตรวจสอบ `DATABASE_URL` ใน `.env` ของ backend

### 3. Next.js Proxy ไม่ทำงาน
- ตรวจสอบ `next.config.mjs` ว่ามี rewrite ถูกต้อง
- Restart Next.js dev server

---

## 📞 ต้องการความช่วยเหลือเพิ่มเติม?

ตรวจสอบ logs ใน:
- **Backend Terminal**: ดู error messages
- **Next.js Terminal**: ดู compilation errors
- **Browser Console**: ดู network errors
